package project.init.framework.config;import io.swagger.v3.oas.annotations.enums.SecuritySchemeType;import io.swagger.v3.oas.annotations.security.SecurityScheme;import io.swagger.v3.oas.models.OpenAPI;import io.swagger.v3.oas.models.info.Info;import io.swagger.v3.oas.models.servers.Server;import org.springframework.beans.factory.annotation.Value;import org.springframework.context.annotation.Bean;import org.springframework.context.annotation.Configuration;@Configuration@SecurityScheme(        name = "bearerAuth", // Tên scheme bảo mật, dùng để tham chiếu trong SecurityRequirement        type = SecuritySchemeType.HTTP,        scheme = "bearer",   // Loại HTTP Bearer        bearerFormat = "JWT" // Định dạng token (JWT))public class SwaggerConfig {    @Value("${server.servlet.contextPath}")    private String contextPath;    @Value("${springdoc.apiDocs.serverUrl}")    private String url;    // Cấu hình info//    Info info = new Info()//            .title("Tutorial Management API")//            .version("1.0")//            .contact(contact)//            .description("This API exposes endpoints to manage tutorials.").termsOfService("https://www.xyz.com/xyz")//            .license(mitLicense);    @Bean    public OpenAPI customOpenAPI() {        return new OpenAPI()                //                .info(new Info().title("Project API Documentation").version("v1.0"))                //                .addServersItem(new Server().url(url + contextPath));    }}