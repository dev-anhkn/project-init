package project.init.framework.error;import lombok.Getter;import project.init.framework.utils.enummerate.EErrorMessage;@Getterpublic final class ApiException extends RuntimeException {    private final int statusCode;    //    private final int value;    private final String message;    public ApiException(EErrorMessage error, int statusCode) {        super();        this.statusCode = statusCode;        //        this.value = error.getValue();        this.message = (String) error.getMessage();    }}// -----------------------------------------------------//	•	RuntimeException là unchecked exception, không bắt buộc throws hoặc try-catch//	•	Dễ tích hợp với @RestControllerAdvice để xử lý lỗi toàn cục//	•	Thường dùng cho lỗi nghiệp vụ (business logic) như: phân quyền, xác thực, validate,…//	•	Gọn code hơn so với Exception (checked)//	•	Spring transaction tự rollback với RuntimeException, không cần cấu hình thêm